name: image_quality_guard
description: Detect blur, brightness, and contrast issues in images with configurable thresholds and presets for reliable capture flows.
version: 1.0.0
homepage: https://github.com/Tanvirul-swe/image_quality_guard
repository: https://github.com/Tanvirul-swe/image_quality_guard
issue_tracker: https://github.com/Tanvirul-swe/image_quality_guard/issues

topics:
  - image-processing
  - image-quality
  - blur-detection
  - validation
  - computer-vision

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  image: ^4.10.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.2
